// Neste exemplo, o Hero foi criado como uma interface. Eu já fiz alguns exemplos onde o foi criado como uma classe
// Descobrir a diferença

export interface Hero {
    id: number;
    name: string;
}